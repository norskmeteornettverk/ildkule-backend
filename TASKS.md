# Tasks

## Terminology Cleanup

- Review public `crossbearing` wording and replace or alias it with `cross_station_confirmed`.
- Review overloaded `confirmed` naming so user state and meteor review state are easier to tell apart.
- Review public user role names for consistency:
  - `role`
  - `user_role`
  - `roles`
- Review token names for consistency:
  - `token`
  - `accessToken`
- Decide later if these should stay as glossary notes or become API rename tasks.
